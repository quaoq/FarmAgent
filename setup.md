#### **Installation**

Prerequisites: `uv` Python package and project manager

```
pip install uv
```

Then run:

```
uv venv -p 3.12

source .venv/bin/activate

uv pip install -r requirements.txt
```